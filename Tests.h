#pragma once

#include "transport_catalogue.h"

namespace transport_catalogue {
    namespace Tests {
        void AddStop();
        void ParseStopRequest();
        //void ParseBusRequest();
        void AllTests();
    }
}